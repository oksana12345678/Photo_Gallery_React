export const errorSelector = (state) => state.booking.error;
export const isLoadingSelector = (state) => state.booking.isLoading;
export const bookSelector = (state) => state.booking.book;
export const dateSelector = (state) => state.booking.date;